<nav class="mt-3">
    <ul class="pagination justify-content-center">
        {% for i in range(1,  countLinks) %}
            <li class="page-item"><a class="page-link" href="/comments?page={{ i }}">{{ i }}</a></li>
        {% endfor %}
    </ul>
</nav>
